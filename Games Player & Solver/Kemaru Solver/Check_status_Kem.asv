function [mistake, finished] = Check_status_Kem(dim, Kem, Cage, Cage_size)
mistake=0;
finished=1;
sol=Kem(:,:,1);

% Check if finished
for i=1:dim(1)
    for j=1:dim(2)
        if Ke(i,j)==0
            finished=0;
        end
    end
end

% Check if two same # neighbors
for i=2:dim(1)
    for j=2:dim(2)
        if Kem(i,j,1)==Kem(i-1,j,1) || Kem(i,j,1)

        end
    end
end


        
        
        
    end
end


end
